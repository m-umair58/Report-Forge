import { DefaultTheme, toLayoutTheme } from '@reportforge/theme';

/**
 * Default layout theme — re-exported from `@reportforge/theme` for backward compatibility.
 */
export const DEFAULT_LAYOUT_THEME = toLayoutTheme(DefaultTheme);
